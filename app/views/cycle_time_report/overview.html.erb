<%= render 'shared/nav_bar' %>
<main role="main" class="container-fluid">
  <div class="main">
    <div class="card box-shadow mb-4">
      <div class="card-header">
        <a href="<%= cycle_time_overview_path(department: @deployment_constraint.id, format: 'csv') %>"
          class="card-link float-right">
          Download as csv file
        </a>
        <strong>2-weekly cycle time averages overview for constraint group: <%= @deployment_constraint.name %></strong><br>
      </div>
      <div class="card-body">
        <%= render partial: 'shared/table', locals: { table: @report } %>
      </div>
   </div>
 </div>
</main>
