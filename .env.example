JIRA_USERNAME=<<your JIRA username>>
JIRA_API_KEY=<<your JIRA api key>>
JIRA_SITE=<<URL of your JIRA site>>
REDIS_HOST=<<Redis host, by default it is 'redis' if you use docker-compose>>
RAILS_RELATIVE_URL_ROOT=<<Redis host, by default it is '' if you use docker-compose, '/jira' when running in kubernetes>>
